
# JWT
jwt.secret=reciclai_super_secret_key_2024_muito_segura_nao_compartilhar
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:3000,http://localhost:8081

# Logging
logging.level.com.example.reciclai=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
spring.application.name=reciclai-backend

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/reciclai
spring.datasource.username=${DB_USER:reciclai_user}
spring.datasource.password=${DB_PASSWORD:reciclai_pass_2024}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080
server.error.include-message=always
server.error.include-stacktrace=never

